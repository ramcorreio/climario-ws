<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">
					
	<http pattern="/javax.faces.resource/**" security="none"/>
	<http pattern="/resources/**" security="none"/>
	<http pattern="/pages/login.jsf" security="none"/>
	<http pattern="/pages/init.jsf" security="none"/>
	<http pattern="/api/**" security="none"/>
	<http pattern="/*.dev" security="none"/>

	<http use-expressions="true">
		<intercept-url pattern="/pages/admin*" access="hasRole('ADMIN')" />
		<intercept-url pattern="/**" access="hasAnyRole('ADMIN','USER')" />
		<form-login login-page="/pages/login.jsf" 
			authentication-failure-url="/pages/login.jsf?error=1" 
			login-processing-url="/pages/monitore_login"
			default-target-url="/pages/home.jsf"
			always-use-default-target="true" />
		<logout invalidate-session="true" delete-cookies="true" logout-success-url="/pages/home.jsf" />
		<csrf disabled="true" />
	</http>
	
	<!-- enable support for Spring Security’s expression-based syntax -->
	<global-method-security pre-post-annotations="enabled" />

	<!-- Select users and user_roles from database -->
	<authentication-manager>
		<authentication-provider>
			<password-encoder ref="bcryptEncoder" />
			<jdbc-user-service 
				data-source-ref="monitoreDS" 
				users-by-username-query="SELECT username, password, enabled FROM app_user WHERE username = ?"
				authorities-by-username-query="SELECT u.username,r.authority FROM app_user u, app_role r, app_user_role j 
					WHERE u.user_id = j.user_id AND r.role_id = j.role_id AND u.username = ?" />
	  	</authentication-provider>
	</authentication-manager>
		
	<beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />	
			
</beans:beans>